## Note to translators:

Due to our heritage of UT/UBPorts, translations are done using GNU GetText/intltool.

So, instead of Linguist and .ts files, use the .pot and .po files in `po/` for translations.

